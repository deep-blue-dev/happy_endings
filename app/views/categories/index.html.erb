<!-- Main jumbotron for a primary marketing message or call to action -->
<div class="row">
  <div class="col-sm-12">
     <div id="homeCarousel" class="carousel slide">
       <!-- Indicators -->
       <ol class="carousel-indicators">
         <li data-target="#homeCarousel" data-slide-to="0" class="active"></li>
         <li data-target="#homeCarousel" data-slide-to="1"></li>
         <li data-target="#homeCarousel" data-slide-to="2"></li>
       </ol>
       <!-- Wrapper for slides -->
       <div class="carousel-inner">
         <div class="item active">
           <img src="http://hdwallpaperbackgrounds.net/wp-content/uploads/2015/09/best-hd-desktop-wallpapers-of-flower.jpg" alt="dandelion">
           <div class="carousel-caption">
             <h1>Headline</h1>
             <p>Here is an example description</p>
           </div>
         </div>
         <div class="item">
           <img src="http://hdwallpapersdesktop.com/HD-Wallpapers211/images/Wallpapers-HD-Widescreen--HQ-Desktop-Background-57.jpg" alt="mountains">
           <div class="carousel-caption">
             <h1>Headline</h1>
             <p>Here is an example description</p>
           </div>
         </div>
         <div class="item">
           <img src="http://hdwallpaperia.com/wp-content/uploads/2013/12/Cat-Desktop-Wallpapers.jpg" alt="astrocat">
           <div class="carousel-caption">
             <h1>Headline</h1>
             <p>Here is an example description</p>
           </div>
         </div>
       </div>
       <!-- Controls -->
       <a class="left carousel-control" href="#homeCarousel" role="button" data-slide="prev">
         <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
         <span class="sr-only">Previous</span>
       </a>
       <a class="right carousel-control" href="#homeCarousel" role="button" data-slide="next">
         <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
         <span class="sr-only">Next</span>
       </a>
    </div>
   </div>
 </div>
<div class="text-center">
  <h1 class="titles">Categories</h1>

  <% @categories.in_groups_of(3) do |group| %>
    <div class="row">
      <% group.compact.each do |category| %>
        <div class="col-sm-12 col-md-4">
          <div class="category-wrapper">
            <%=link_to( image_tag( category.image, size: "200x200", class: "img-responsive img-margin" ),
                      category_products_path(category)) if category.image.present? %>
            <h4><%= link_to category.name, category_products_path(category) %></h4>
            <% if admin_signed_in? %>
              <%= link_to 'Show', category, class: "btn btn-primary" %>&nbsp;
              <%= link_to 'Edit', edit_category_path(category), class: "btn btn-basecamp" %>&nbsp;
              <%= link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>

  <%# if admin_signed_in? %>
  <div class="pad-btm">
    <%= link_to 'New Category', new_category_path, class: "btn btn-basecamp" %>
  </div>
  <%# end %>
</div>
